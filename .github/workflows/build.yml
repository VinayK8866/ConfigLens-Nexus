name: Eclipse Plugin CI

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean verify -Pjgit-ts

    - name: Package Update Site
      if: success() && github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      run: |
        mvn package -DskipTests --projects com.configlens.updatesite -am -Pjgit-ts
        
        # Define destination
        DEST="com.configlens.updatesite/target/repository"
        
        # Inject premium landing page assets
        cp -rv com.configlens.updatesite/web/* "$DEST/"
        
        # Disable Jekyll processing to ensure index.html and CSS are used directly
        touch "$DEST/.nojekyll"
        
        echo "Deployment directory prepared: $DEST"
        ls -R "$DEST"

    - name: Deploy to GitHub Pages
      if: success() && github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./com.configlens.updatesite/target/repository
        force_orphan: true
